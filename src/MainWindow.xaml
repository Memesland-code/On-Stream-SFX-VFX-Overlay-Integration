<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="On_Stream_SFX_VFX_Overlay_Integration.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:On_Stream_SFX_VFX_Overlay_Integration"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="On-Stream-SFX-VFX-Overlay-Integration">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Page Name="TESTING">
        <StackPanel Margin="20">
            <Button Content="Add and audio or video file" Click="PickFile_Click"/>
            <MediaPlayerElement x:Name="PlayerControl"
                                AreTransportControlsEnabled="False"
                                AutoPlay="False"
                                Stretch="Uniform"
                                Width="400" Height="300"/>

            <ListView x:Name="MediaListView"
                      Height="300"
                      SelectionMode="Single"
                      SelectionChanged="MediaListView_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ButtonInstance">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{x:Bind Name}"
                                       VerticalAlignment="Center"
                                       FontSize="16"/>
                            
                            <Button Grid.Column="1"
                                    Content="Play test"
                                    Click="TestPlay_Click"
                                    Tag="{x:Bind}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button x:Name="AddToListButton"
                    Content="Add to list"
                    Click="AddToList_Click"
                    Margin="0, 12, 0, 0"/>

            <TextBlock x:Name="StatusText"/>
        </StackPanel>
    </Page>
</Window>
